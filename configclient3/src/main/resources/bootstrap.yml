spring:
    cloud:
        config:
            label: master
#            uri: http://localhost:8889
            name: config-client-dev,default
            discovery:
              enabled: true
              service-id: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://server2:1113/eureka/,http://server1:1112/eureka/

# 需要注意的是，config-client-dev 直接写git上的文件名
